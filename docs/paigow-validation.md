# 牌九撲克驗證紀錄

日期：2026-09-12。規格：GitHub #11、#12。
實作提交：13734d99a1e3faa8b3937f29ba0aa95cadfe5f7e。
Review 補強提交：df8ef88（只新增固定牌例及規則說明，未修改產品程式）。

## 自動化結果

- Windows、Python 3.10.6、專案 .venv；discord.py 2.7.1、mypy 2.3.1。
- 完整套件：RUN_DB_TESTS=1，python -m unittest discover -s tests -q。
  實作提交上的 151 項測試全部通過，耗時 600.605 秒，exit 0，無 skip。
  含既有骰子、二十一點、水晶、獎勵、查帳及連線恢復回歸。
- Review 後新增五個固定 House Way 牌例；在 df8ef88 內容上重新執行
  test_paigow.py 的 7 項規則測試，全部通過。測試方法總數不變。
- 牌九隔離資料庫切面：7 項全部通過，涵蓋原子扣款／派彩、提交前失敗與提交後回應遺失、
  逾時確認競態、重啟、重送／再開、非法狀態退款、大額整數及 Discord 文字端到端流程。
- 牌九 UI 切面：5 項全部通過，含牌桌可解碼、全萬用 Joker 素材、文字與上傳回退、
  owner guard、舊版本圖片不可覆蓋新選牌、active 圖片暗牌遮蔽。
- mypy --follow-imports=silent --ignore-missing-imports --check-untyped-defs
  paigow.py casino_store.py casino_commands.py casino_images.py：四檔通過。
- 差異空白檢查通過；變更檔案 UTF-8 嚴格解碼通過，無新增 U+FFFD 取代字元。

資料庫測試使用測試 fixture 建立的隨機隔離 schema，測試後清除；沒有遷移正式帳戶資料。
輸出中的圖片失敗、網路失敗、連線失效等 warning 來自故障注入情境，不表示測試失敗。

## 圖片驗證

生成初始選牌、已選前二後五及結算預覽；目視檢查已選與結算圖片。
1200×800 牌桌中，手牌編號、Joker 及代表牌、雙墩牌型、勝負與金額未見裁切或重疊。
這是本機圖片驗證，不能證明 Discord 手機客戶端的實際呈現。

## Standards

書面規範違反：0。判斷性建議：1（低優先）。
House Way 候選分墩使用 tuple 與數字牌型索引；可改為具名候選結構與牌型常數，
減少修改規則時解讀索引的負擔。這是可讀性建議，不是硬性違規或已確認功能錯誤，
本次未另做結構重整。

## Spec

Review 最初指出固定牌例缺少順子／同花重疊及 Joker 改變分組的情境。
已新增五例、補充 Joker 分類與實際替代可以不同的說明，並通過針對性測試。
補驗沒有發現剩餘可重現的 Joker 評分、派彩、遮蔽、固定期限或跨遊戲互斥錯誤。

仍有 1 項已知验收限制：#12 的 Discord 桌面／手機實機驗收未完成。
使用者已明確選擇「先保留實機驗收待辦」；#12 保持開啟並標為 ready-for-human。
包含手機辨識性的第一項及實機驗收項不以本機圖片或互動替身勾選完成。

Review 結果：Standards 0 硬性違規、1 非阻擋建議；Spec 0 未修功能／測試缺口、1 已接受的實機驗收待辦。

## A–K 排序、直式牌桌與延遲改善（2026-09-12）

主要實作提交：0829e6b，另補上已受理圖片更新的序列化防護。

- 手牌、選單、文字、圖片統一 A–K 排序，Joker 最後；提交仍使用原實體牌 ID。
- 牌桌改為 1200×1800，前 2 張在上、後 5 張在下，單牌 168×233。
  已生成未分墩、已分墩及結算預覽，目視檢查最終結算圖沒有牌型文字與下一墩標題重疊。
- 交易提交後先更新文字及操作控制，再背景補圖。合圖中的舊工作可取消；
  已送出的 Discord 圖片編輯保持序列化，完成後才套用新預覽。
- 選牌減少一次資料庫往返；重選同兩張牌不寫入或增加版本，固定期限不變。
- 完整非資料庫回歸：RUN_DB_TESTS=0，共發現 155 項，實際執行 96 項全部通過，
  59 項資料庫測試依設定略過，耗時 2.699 秒。
- 受影響的牌九隔離資料庫切面另以 RUN_DB_TESTS=1 執行，8 項全部通過，87.186 秒。
  包含新增同組牌順序相反的 no-op 回歸，以及原有交易、期限、退款與文字端到端驗證。
  最後的圖片交付修補沒有修改資料庫程式，未重跑未受影響的其他資料庫切面。
- 牌九 UI 8 項通過：新增選牌先顯示、排序及已受理舊上傳序列化回歸。
  在途請求測試先於未修程式重現失敗，再於修補後通過，驗證舊圖片不會覆蓋新預覽。
- 五個受影響產品模組的 mypy 通過。
- 本機三次選牌資料庫樣本中位数約 560 → 487 ms；
  最終 1200×1800 圖片暖合圖 12 次中位數 42.40 ms，309800 bytes。
  圖片放大增加合圖成本，但已移至背景；這些數字不含真實 Discord 上傳或客戶端耗時。
- 本輪 Standards：0 書面違規、0 新增結構建議。
  Spec 發現 1 項在途舊圖片覆蓋風險，已修復、加入可重現回歸並複審通過；無剩餘功能缺口。

#12 的 Discord 桌面／手機實機驗收依使用者決定繼續保留待辦，未發送實機測試訊息。

## 恢復並排牌桌（2026-09-12）

依使用者後續要求，牌桌恢復原本 1200×800、單牌 112×156 的並排設計：前二在左、後五在右。
A–K 排序、手牌編號、非同步補圖及在途圖片序列化防護保留。
牌九 UI 8 項測試通過（0.470 秒），已生成初始、選牌及結果圖，目視檢查結算圖無文字重疊。
本次僅調整 renderer、圖片尺寸斷言與文件；資料庫及結算程式未變更，實機驗收仍待辦。

## Joker 結算排序與傳統牌面（2026-09-12）

結算後，玩家與莊家的前後墩圖片、文字依 Joker 最佳代表牌的 A–K 點數及花色排序；
代表牌與實牌相同時，實牌在前。Joker 的實體 ID、原手牌編號及變牌說明保留。
選牌階段仍將 Joker 放最後。使用內建 image_gen 生成傳統紅黑金小丑牌，
原始 PNG 保存為 casino_assets/classic/cards/52.png，提示詞與來源紀錄隨素材保存。
已目視檢查結算預覽，Joker 當 A♠ 時排於 A♥ 前，牌面及變牌標示可辨識。
新增規則牌例涵蓋順子、五條、前墩對子及皇家同花順；UI 驗證雙方結算圖片位置與文字對應。
完整非資料庫回歸發現 157 項，98 項通過、59 項資料庫測試依設定略過（2.896 秒）；
三個產品模組 mypy 通過。未修改資料庫或結算評分邏輯，實機驗收仍保留待辦。
