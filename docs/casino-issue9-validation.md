# Issue #9 驗證紀錄

日期：2026-09-06。基準：`dca0b16f537ece9b7698a4ebc6e7be994a7b7eae`，原工作區乾淨。

## 交付範圍

- 大廳增加我的紀錄／莊家統計；私人查詢另發 ephemeral，不改寫公開大廳。
- 授權者另收到私人管理入口，透過玩家 ID／牌局 UUID Modal 查詢，沒有新增 slash 指令。
- CasinoRecords 以持久流水重算，統計依遊戲與狀態分組，明細使用明確欄位投影與一致性交易。
- 查詢含 dice、blackjack 及其他已記錄識別；完成／進行中／退款分離，保留 dismissed 歷史。
- 按鈕、Modal、分頁重驗身分與目前權限，查詢完成後再確認權限；更新後舊頁失效。
- 沿用現有 schema，無新增遷移、資金操作、清理工作或資料刪除。

## TDD 與局部驗證

沿用父規格已確認的公開操作／查詢、Discord interaction 替身、隔離 PostgreSQL 三個邊界。

- 第一個真實流水查詢案例先以缺少 CasinoRecords 失敗，實作後通過（20.020 秒）。
- 大廳私人回覆、分頁／舊頁、管理入口及撤權各切片先觀察失敗再通過。
- 莊家勝負顯示方向的回歸先失敗，再修正為玩家勝負相反。
- 新增三項 PostgreSQL 整合測試全數通過（111.767 秒），使用自行建立並清除的 casino_test_<UUID> schema。
  覆蓋真實骰子勝／負／平、21 點天然／加倍、損壞退款、進行中、未知遊戲識別、分頁、
  重啟查詢、dismissed 保留、流水核對、私人與公開 UI、暗牌排除及猜他人牌局 ID。
- 六項新 UI 契約測試全數通過（0.133 秒），涵蓋另發 ephemeral、分頁／篩選、舊頁、
  他人點擊（含其他授權者）、錯誤／未知 ID、Bot 擁有者、一般管理員不得查帳、
  Modal 撤權及查詢途中撤權。
- 六個相關模組 mypy 通過；修改檔案嚴格 UTF-8 解碼及替代字元檢查通過。

## Standards

以固定基準的全部變更委派獨立子代理審查；0 項硬性違規、0 項需處理的啟發式建議。

## Spec

以 Issue #9 與父規格委派另一獨立子代理審查；0 項缺漏、錯誤實作或超出範圍的發現。

## 未執行的部署驗收

正式資料庫備份／遷移、Discord 手機／桌面與實際網路延遲驗收仍由 Issue #10 追蹤。
本次未啟動正式 Bot，也未對 public schema 寫入資料；替身與隔離測試不代表實機驗收通過。

## 完整測試

執行 `RUN_DB_TESTS=1 python -m unittest discover -s tests -v`：
86 項全數通過，耗時 928.371 秒，0 失敗、0 skip。
其中 37 項為真實 PostgreSQL 測試（原有 34 項加上 #9 新增 3 項），其餘 49 項為規則／UI／設定等測試。
完整執行開始後沒有修改程式或測試；只有整理操作、交接與本驗證文件。
執行中圖片／訊息失敗警告是既有受控故障案例，結果均通過。
